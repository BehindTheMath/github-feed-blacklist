{
  "private": true,
  "engines": {
    "node": ">=0.8.0"
  },
  "name": "GithubFeedBlacklist",
  "scripts": {
    "zip": "node scripts/zip.js"
  },
  "devDependencies": {
    "zip-folder": "^1.0.0"
  },
  "eslintConfig": {
    "env": {
      "node": true,
      "browser": true
    },
    "globals": {
      "chrome": true
    },
    "rules": {
      "eol-last": 0,
      "quotes": [
        2,
        "single"
      ]
    }
  }
}
